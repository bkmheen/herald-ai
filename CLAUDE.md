# CLAUDE.md — herald-ai 작업 규칙 (단일 출처)

이 파일은 Claude Code 가 프로젝트 진입 시 자동으로 읽는 규칙 문서입니다.
**herald-ai 저장소의 git 운영 규칙은 이 파일이 단일 출처(single source of truth)** 이며,
git 으로 배포되므로 어느 Mac 에서 clone/설치하든 동일하게 적용됩니다.
(참고: `~/.claude/.../memory/` 의 프로젝트 메모리는 머신 로컬이라 전파되지 않습니다.
그래서 "여러 Mac 에서 동일 적용" 이 필요한 규칙은 반드시 저장소 안 이 파일에 둡니다.)

---

## 핵심 포인트

- **버전 관리**: `major.minor.patch` 3단이되 **`patch = 묶음×10 + 커밋(1자리)`** (규약 v3.1.0).
  **묶음은 주제가 아니라 10커밋 구간**이다 — 주제는 태그와 DEVLOG 가 잇는다.
  `minor` 는 사용자 허락, `major` 는 사용자 지시. **전 이력이 이 체계다** (2026-08-16 소급 재번호).
- **커밋**: 매 수정마다 강제는 아니나, 실행 전·요청 시 반드시 커밋 선행.
  모든 `patch` 에는 최소한의 커밋 기록을 남김.
- **개발 기록**: README / CHANGELOG / DEVLOG 표준 기록 유지, 파일 비대 시 주제별
  분할(`README_subtitle.md`)하고 전체 구조는 README 에 남겨 연결.
- **푸시**: 자동 금지, 사용자 요청 시에만.

---

## 상세 내용

### 버전 관리 규칙 (규약 v3.1.0 체계 — 전 이력 적용)

형식은 `major.minor.patch` 3단이되, **`patch` 는 묶음 번호 뒤에 한 자리 커밋 번호를 붙인 값**이다.

```
patch = 묶음 × 10 + 커밋          예)  0.2.10   = 묶음 1 · 커밋 0
        └ 1~ (상한 없음) ┘ └ 0~9 ┘      0.2.247  = 묶음 24 · 커밋 7
                                       0.2.1230 = 묶음 123 · 커밋 0
```

**읽는 법은 언제나 같다 — 맨 뒤 한 자리가 커밋, 그 앞이 전부 묶음이다.**
묶음에 상한이 없으므로 자리가 모자라면 patch 가 길어질 뿐이고, 그 때문에 minor 를 올리지 않는다.

| 자리 | 내부 이름 | 부르는 말 | 올리는 주체 |
|------|-----------|----------|-------------|
| `major` | major | — | **사용자 지시** |
| `minor` | minor | — | **사용자 허락** (Claude 는 근거를 들어 요청만 한다) |
| 일의 자리를 뺀 나머지 | patch | **묶음** | Claude 자체 판단 |
| 일의 자리 | subpatch | **커밋** | **커밋마다 +1** |

#### 묶음은 주제가 아니라 **10커밋 구간**이다

> ⚠️ 커밋이 **9**에 닿으면 **주제와 무관하게** 다음 묶음으로 넘어간다.
> **작업 주제를 잇는 것은 마일스톤 태그와 [`DEVLOG.md`](DEVLOG.md) 다.**
> 한 주제가 묶음 여러 개에 걸치는 것은 정상이며, 태그가 그 구간 전체를 가리킨다.

- 묶음의 첫 커밋은 **`x0`** (묶음 2 시작 = `20`)
- **커밋 9 도달 → 묶음 강제 전환** (`19` 다음은 `20`) — 자리 침범 방지
- 주제가 바뀌면 커밋이 9가 아니어도 묶음을 넘길 수 있다 (선택)
- **minor 가 오르면 patch 는 항상 `10`(묶음 1·커밋 0)에서 시작한다.** `x.y.0` 은 쓰지 않는다 —
  `x.y.0~x.y.9` 는 "묶음 0" 이라 정의되지 않는 구간이다
- **zero-padding 없음** (semver 가 leading zero 를 금지한다). 문서에서도 `묶음 24` 로 적는다
- **버전은 단조 증가한다.** 지나온 값으로 되돌아가지 않는다
- **규칙을 또 바꾸면 그때도 minor 를 올려 경계를 만든다** — 같은 minor 안에서 patch 의 뜻이
  바뀌면 과거 번호를 해석할 수 없다
- 현재 버전의 단일 출처는 [`VERSION`](VERSION) 파일. README 등 문서의 버전 표기는
  이 파일과 일치시킨다

#### 이력 — 전 구간이 한 체계다 (2026-08-16 소급 재번호)

**최초 커밋부터 지금까지가 전부 이 체계로 매겨져 있다.** 해석표가 필요 없다.

| 구간 | 묶음 | 시기 |
|---|---|---|
| `0.2.10`~`0.2.19` | 묶음 1 | 2026-06-20 ~ 08-01 |
| `0.2.20`~`0.2.29` | 묶음 2 | 2026-08-01 ~ 08-16 |
| `0.2.30`~`0.2.39` | 묶음 3 | 2026-08-16 |
| `0.2.40`~ | 묶음 4~ | 2026-08-16 ~ |

2026-08-16 에 규약 v3.1.0 을 도입하면서 **1회 마이그레이션**으로 옛 번호
(`0.1.0`~`0.9.202`, 세 가지 서로 다른 체계가 섞여 있었다)를 전부 이 체계로 다시 매겼다.
커밋 메시지·문서·태그를 함께 고쳐 **어디에도 역행 구간이 없다.**

**앞으로는 소급 재번호를 하지 않는다.** 규칙이 또 바뀌면 **minor 를 올려** 경계를 만든다 —
이미 push 된 히스토리를 다시 쓰는 것은 위험 대비 실익이 없고, 이번은 체계 도입 시점의 예외였다.

#### 기록 위치

- `x0` 커밋의 CHANGELOG 머리말: `> **묶음 N — <제목>** (slug: <slug>)`
- [`DEVLOG.md`](DEVLOG.md) 상단 "현재 묶음" 줄 갱신
- **주제가 끝날 때 태그**: `git tag -a milestone/<slug> -m "<설명> (0.2.23~0.2.36)"`
  — 묶음 경계가 아니라 **주제 경계**에 붙인다. 한 태그가 묶음 여러 개를 아우를 수 있다
- 세부 구분은 커밋 제목의 `[slug]` 라벨로

### 커밋 메시지 제목

```
v<버전> [<slug>] <한국어 요약>
```

`[slug]` 는 세부 작업 구분 라벨 — 예: `[docs]` `[vault]` `[index]` `[convention]` `[install]` `[privacy]`.

트레일러는 **`Co-Authored-By` 한 줄만** 둔다. 규약 v2.0.0 §3.3 의 `Worked-On`·`Committed-To` 는
호스트 이름과 IP 를 커밋 메시지에 남기므로 **이 공개 저장소에는 쓰지 않는다** (비공개 저장소에는 규약대로).

### 공개 저장소 원칙 — 도구는 공개, 구성은 비공개 (2026-08-16 확정)

**herald-ai 는 배포되는 패키지다.** 여기에 들어가는 것은 *패키지를 쓰는 법* 이고,
*누가 어디서 어떤 주소로 쓰는가* 는 각자의 `~/.herald/vault.conf` 와 자기 vault 에 있다.

문서를 쓰거나 예시를 넣기 전에 **먼저 성격을 판정한다.**

| 판정 | 어디로 |
|---|---|
| 패키지를 쓰는 법·설계 근거 | herald-ai (공개) |
| 내 서버 주소·호스트 이름·개인 경로·구축 이력 | vault (비공개) |

**공개 저장소에 넣지 않는 것** — IP·호스트 이름·개인 계정과 홈 경로·사용 금액·
"내가 어떻게 깔았는가" 의 진행 기록. 예시가 필요하면 `<vault-server>`·`laptop`·`/home/you/…`
같은 **자리표시자**를 쓰고, 실제 값은 **설정 항목으로 안내**한다
(표본: [`config/vault.conf.example`](config/vault.conf.example)).

스크립트에 **내 환경 값을 기본값으로 박지 않는다.** 처음 쓰는 사람이 엉뚱한 곳으로 붙는다.

### 커밋 규칙

- 수정할 때마다 커밋할 필요는 없음.
- 단, 실행 전 또는 요청 받았을 때는 지금까지의 수정 내용을 커밋한 후 실행.
- 커밋 메시지 말미에 공동 작성자 트레일러를 남긴다:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### 커밋 시 개발 기록 규칙

- README, CHANGELOG, DEVLOG 등 표준에 따라 기록 — 향후 참고 가능하도록 매 수정마다 작성.
- 한 파일이 너무 커지면 `README_subtitle.md` 형태로 주제별 분할.
- 전체 구조는 README 에 남겨 서브 파일의 존재를 알림.
- CHANGELOG, DEVLOG 도 동일한 분할·연결 방식으로 관리.

### 푸시 규칙

- `push` 는 자동으로 하지 않음.
- 필요 시 사용자가 별도로 요청.
