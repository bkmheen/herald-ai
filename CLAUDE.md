# CLAUDE.md — herald-ai 작업 규칙 (단일 출처)

이 파일은 Claude Code 가 프로젝트 진입 시 자동으로 읽는 규칙 문서입니다.
**herald-ai 저장소의 git 운영 규칙은 이 파일이 단일 출처(single source of truth)** 이며,
git 으로 배포되므로 어느 Mac 에서 clone/설치하든 동일하게 적용됩니다.
(참고: `~/.claude/.../memory/` 의 프로젝트 메모리는 머신 로컬이라 전파되지 않습니다.
그래서 "여러 Mac 에서 동일 적용" 이 필요한 규칙은 반드시 저장소 안 이 파일에 둡니다.)

---

## 핵심 포인트

- **버전 관리**: `major.minor.patch` 3단이되 **`patch = 묶음×100 + 커밋`** (규약 v2.0.0).
  주제가 바뀌면 묶음↑(`x00`), 이어지는 작업은 커밋↑. `minor` 는 사용자 허락, `major` 는 사용자 지시.
  **v0.2.37 부터 적용.**
- **커밋**: 매 수정마다 강제는 아니나, 실행 전·요청 시 반드시 커밋 선행.
  모든 `patch` 에는 최소한의 커밋 기록을 남김.
- **개발 기록**: README / CHANGELOG / DEVLOG 표준 기록 유지, 파일 비대 시 주제별
  분할(`README_subtitle.md`)하고 전체 구조는 README 에 남겨 연결.
- **푸시**: 자동 금지, 사용자 요청 시에만.

---

## 상세 내용

### 버전 관리 규칙 (규약 v2.0.0 체계, **v0.2.37~ 적용**)

형식은 `major.minor.patch` 3단이되, **`patch` 는 두 자리 수 두 개를 합친 값**이다.
vault `conventions/git/v2.0.0.md` §4.1 의 체계를 herald-ai 에도 적용한다 (사용자 지시 2026-08-16).

```
patch = 묶음 × 100 + 커밋          예)  0.9.203  =  묶음 02 · 커밋 03
        └ 01~99 ┘   └ 00~99 ┘
```

| 자리 | 내부 이름 | 부르는 말 | 올리는 주체 |
|------|-----------|----------|-------------|
| `major` | major | — | **사용자 지시** |
| `minor` | minor | — | **사용자 허락** (Claude 는 근거를 들어 요청만 한다) |
| 백의 자리 | patch | **묶음** | **Claude 판단** — 작업 주제가 바뀔 때 |
| 끝 두 자리 | subpatch | **커밋** | **커밋마다 +1** |

- 묶음의 첫 커밋은 **`x00`** (묶음 02 시작 = `200`). herald-ai 도입 시작점은 **`0.2.37`**
- **커밋 99 도달 → 묶음 강제 전환** (`299` 다음은 `300`)
- **묶음 99 도달 → 사용자께 minor 승격 요청.** 승격 시 patch 는 `100` 으로 리셋
- **zero-padding 없음** — `0.9.203`. 자릿수는 문서 표기에서만 ("묶음 02")
- **버전은 단조 증가** · 한 묶음의 커밋 수 목표 **10~40**
- 현재 버전의 단일 출처는 [`VERSION`](VERSION) 파일. README 등 문서의 버전 표기는
  이 파일과 일치시킨다.

**묶음을 넘기는 판정** — 새 기능 착수 · 성격이 다른 영역으로 전환 · 인프라/빌드 체계 변경 ·
사용자가 새 주제를 지시. 넘기지 않는 경우: 같은 묶음의 후속 수정, 직전 작업의 버그 수정, 문서 갱신
→ 전부 커밋(subpatch) 증가.

**묶음마다 기록** — `x00` 커밋의 CHANGELOG 머리말에 `> **묶음 NN — <제목>** (slug: <slug>)`,
[`DEVLOG.md`](DEVLOG.md) 상단 "현재 묶음" 줄 갱신.

### 커밋 메시지 제목

```
v<버전> [<slug>] <한국어 요약>
```

`[slug]` 는 세부 작업 구분 라벨 — 예: `[docs]` `[vault]` `[index]` `[convention]` `[install]` `[privacy]`.

트레일러는 **`Co-Authored-By` 한 줄만** 둔다. 규약 v2.0.0 §3.3 의 `Worked-On`·`Committed-To` 는
호스트 이름과 IP 를 커밋 메시지에 남기므로 **이 공개 저장소에는 쓰지 않는다** (비공개 저장소에는 규약대로).

### 공개 저장소 원칙 — 도구는 공개, 구성은 비공개 (2026-08-16 확정)

**herald-ai 는 배포되는 패키지다.** 여기에 들어가는 것은 *패키지를 쓰는 법* 이고,
*누가 어디서 어떤 주소로 쓰는가* 는 각자의 `~/.herald/vault.conf` 와 자기 vault 에 있다.

문서를 쓰거나 예시를 넣기 전에 **먼저 성격을 판정한다.**

| 판정 | 어디로 |
|---|---|
| 패키지를 쓰는 법·설계 근거 | herald-ai (공개) |
| 내 서버 주소·호스트 이름·개인 경로·구축 이력 | vault (비공개) |

**공개 저장소에 넣지 않는 것** — IP·호스트 이름·개인 계정과 홈 경로·사용 금액·
"내가 어떻게 깔았는가" 의 진행 기록. 예시가 필요하면 `<vault-server>`·`laptop`·`/home/you/…`
같은 **자리표시자**를 쓰고, 실제 값은 **설정 항목으로 안내**한다
(표본: [`config/vault.conf.example`](config/vault.conf.example)).

스크립트에 **내 환경 값을 기본값으로 박지 않는다.** 처음 쓰는 사람이 엉뚱한 곳으로 붙는다.

### 커밋 규칙

- 수정할 때마다 커밋할 필요는 없음.
- 단, 실행 전 또는 요청 받았을 때는 지금까지의 수정 내용을 커밋한 후 실행.
- 커밋 메시지 말미에 공동 작성자 트레일러를 남긴다:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### 커밋 시 개발 기록 규칙

- README, CHANGELOG, DEVLOG 등 표준에 따라 기록 — 향후 참고 가능하도록 매 수정마다 작성.
- 한 파일이 너무 커지면 `README_subtitle.md` 형태로 주제별 분할.
- 전체 구조는 README 에 남겨 서브 파일의 존재를 알림.
- CHANGELOG, DEVLOG 도 동일한 분할·연결 방식으로 관리.

### 푸시 규칙

- `push` 는 자동으로 하지 않음.
- 필요 시 사용자가 별도로 요청.
